import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({

    spinner: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
    }
}));